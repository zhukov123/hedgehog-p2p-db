[package]
name = "hedgehog-metadata-sql"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hedgehog-metadata-core = { path = "../hedgehog-metadata-core" }
hedgehog-types = { path = "../hedgehog-types" }

[lib]
path = "src/lib.rs"

