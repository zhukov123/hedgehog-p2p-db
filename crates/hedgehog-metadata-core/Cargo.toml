[package]
name = "hedgehog-metadata-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hedgehog-types = { path = "../hedgehog-types" }

[lib]
path = "src/lib.rs"

